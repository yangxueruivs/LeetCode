class Solution:
    def findDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        if not nums:
            return -1
        
        nums = sorted(nums)
        for i in range(len(nums)):
            if nums[i] == nums[i+1]:
                return nums[i]
        
        return -1
